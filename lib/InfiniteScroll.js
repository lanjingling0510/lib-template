/* eslint-disable */
import React, { Component } from 'react';
 class InfiniteScroll extends Component {
     render() {
         const style = {
             width: 100,
             height: 100,
             background: 'red',
         };
         return (
             <div id="InfiniteScroll" style={style}></div>
         );
     }
 }

export default InfiniteScroll;
